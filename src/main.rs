fn main() {
    // TODO: methods to:
    //     - send message using api
    //     - receive message using api
    //     - get time remaining in session
    //     - get startup command
    //     - (ground) set startup command
    //     - (ground) set time remaining in session (if not something that can be determined on board)
    println!("Hello, world!");
}
